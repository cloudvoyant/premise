#!/usr/bin/env bash
#MISE description="Print current version"
set -euo pipefail
cd "${MISE_PROJECT_ROOT:-$PWD}"
python3 -c "import tomllib; f=open('pyproject.toml','rb'); d=tomllib.load(f); print(d['project']['version'])"
